# CI/CD Pipeline Generator

Автоматический генератор конфигураций CI/CD пайплайнов для различных языков программирования и фреймворков. Инструмент анализирует структуру проекта и создает готовые к использованию конфигурации для Jenkins, GitLab CI и Docker.

## Описание

CI/CD Pipeline Generator - это инструмент, который анализирует ваш Git-репозиторий и автоматически создает готовые к использованию конфигурации для популярных CI/CD систем:

- **Jenkins** (Jenkinsfile)
- **GitLab CI** (.gitlab-ci.yml)
- **Docker** (Dockerfile и .dockerignore)

Поддерживаемые языки программирования:
- **Java** (Maven, Gradle)
- **Python** (pip, Poetry, pyproject.toml)
- **Node.js/TypeScript** (npm, yarn, pnpm)
- **Go** (Go Modules)
- **Kotlin** (Gradle)
- **C#/.NET** (dotnet)

## Функциональность

- **Автоматический анализ** структуры проекта и зависимостей
- **Обнаружение тестовых фреймворков** (JUnit, pytest, Jest, TestNG, Mocha, и др.)
- **Генерация Dockerfile** с оптимизацией под конкретный язык и фреймворк
- **Анализ и рекомендации** по улучшению структуры проекта
- **Контроль безопасности** - анализ только публичных репозиториев с проверкой URL
- **Детализированные отчеты** в форматах JSON и YAML
- **Интерактивный режим** для удобства использования
- **CLI интерфейс** для интеграции в существующие процессы автоматизации

## Установка и запуск

### Требования
- .NET 8.0 или новее
- Git (для клонирования репозиториев)
- Docker (опционально, для запуска контейнеров)

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd Ci_Cd
```

2. Соберите проект:
```bash
dotnet build --configuration Release
```

### Использование

#### Интерактивный режим (рекомендуется)

Запустите без параметров для интерактивного режима:

```bash
cd Ci_Cd
dotnet run
```

Следуйте инструкциям на экране:
1. Введите URL вашего GitHub репозитория
2. Дождитесь завершения анализа
3. Получите готовые файлы в папке `output/` и архив `output.zip`

#### Командная строка

```bash
# Базовое использование
dotnet run -- --repo https://github.com/user/project

# С указанием выходной папки
dotnet run -- --repo https://github.com/user/project --output ./my-pipelines

# Создание ZIP-архива
dotnet run -- --repo https://github.com/user/project --format zip

# С выполнением тестов (экспериментально)
dotnet run -- --repo https://github.com/user/project --execute
```

#### Параметры командной строки

| Параметр | Сокращение | Описание | По умолчанию |
|----------|------------|----------|--------------|
| `--repo` | `-r` | URL Git-репозитория (обязательно) | - |
| `--output` | `-o` | Папка для сохранения файлов | `./output` |
| `--format` | `-f` | Формат вывода: `dir` или `zip` | `dir` |
| `--execute` | `-e` | Выполнить сборку и тесты | `false` |

## Структура выходных файлов

После анализа создаются следующие файлы:

```
output/
├── .gitlab-ci.yml          # GitLab CI конфигурация
├── Jenkinsfile             # Jenkins pipeline
├── Dockerfile              # Docker образ
├── .dockerignore          # Исключения для Docker
├── analysis-report.json   # Детальный отчет (JSON)
├── analysis-report.yaml   # Детальный отчет (YAML)
├── validation-report.txt  # Результаты валидации
└── DRY_RUN_PREVIEW.md     # Превью и рекомендации
```

## Примеры использования

### Анализ Java проекта с Maven

```bash
dotnet run -- --repo https://github.com/spring-projects/spring-petclinic
```

**Результат:**
- Обнаружит Java + Maven + Spring Boot
- Создаст Jenkinsfile с этапами: checkout, build, test, docker build
- Сгенерирует оптимизированный Dockerfile для Spring Boot
- Добавит кеширование Maven зависимостей

### Анализ Python проекта с Django

```bash
dotnet run -- --repo https://github.com/django/django --output ./django-ci
```

**Результат:**
- Определит Python + Django + pytest
- Создаст пайплайн с установкой зависимостей и тестами
- Предложит добавить coverage.py для покрытия кода

### Анализ Node.js проекта

```bash
dotnet run -- --repo https://github.com/facebook/react --format zip
```

**Результат:**
- Найдет package.json, определит фреймворк React
- Создаст конфигурации с npm ci, build и test этапами
- Упакует все в ZIP-архив

## 📊 Поддерживаемые фреймворки и инструменты

| Язык | Менеджеры пакетов | Фреймворки | Тестирование |
|------|------------------|------------|--------------|
| **Java** | Maven, Gradle | Spring Boot, Quarkus, Micronaut | JUnit, TestNG |
| **Python** | pip, Poetry | Django, Flask, FastAPI | pytest, unittest |
| **Node.js** | npm, yarn, pnpm | React, Vue, Angular, Express, NestJS | Jest, Mocha, Vitest |
| **Go** | Go Modules | Gin, Echo, Fiber | testing, Testify |
| **Kotlin** | Gradle | Spring Boot, Ktor | JUnit, Kotest |
| **C#** | NuGet | ASP.NET Core | MSTest, xUnit, NUnit |

## 🐳 Docker поддержка

Генератор может поднять локальную CI/CD инфраструктуру:

```bash
docker-compose -f docker-compose.simple.yml up -d
```

Это запустит:
- **Jenkins** - для выполнения пайплайнов
- **SonarQube** - для анализа качества кода  
- **Nexus** - для хранения артефактов

## 🔒 Безопасность

- Анализируются только **публичные репозитории**
- Автоматическая проверка URL на приватные IP-адреса
- Sandbox-режим для выполнения команд (опционально)
- Ограничение времени выполнения операций

## 🏗️ Архитектура

Проект состоит из модульных сервисов:

- **AnalyzerService** - анализ структуры проекта
- **GitService** - работа с Git репозиториями
- **TemplateService** - генерация конфигураций из шаблонов
- **DockerfileGenerator** - создание Docker файлов
- **SecurityService** - проверки безопасности
- **SandboxService** - изолированное выполнение команд

## 🤝 Участие в разработке

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения с тестами
4. Создайте Pull Request

### Добавление поддержки нового языка

1. Добавьте детектор в `AnalyzerService.cs`
2. Создайте шаблон в `templates/jenkins/` и `templates/gitlab/`
3. Обновите `DockerfileGenerator.cs`
4. Добавьте тесты

## 📝 Примеры шаблонов

### Сгенерированный Jenkinsfile (Java)

```groovy
pipeline {
    agent { docker { image 'maven:3.9-eclipse-temurin-17' } }
    stages {
        stage('Checkout') { steps { checkout scm } }
        stage('Build') { steps { sh 'mvn clean package -DskipTests' } }
        stage('Test') { steps { sh 'mvn test' } }
        stage('Docker Build') { 
            steps { sh 'docker build -t myapp:${BUILD_NUMBER} .' }
        }
    }
}
```

### Сгенерированный .gitlab-ci.yml (Python)

```yaml
stages:
  - deps
  - test  
  - package

cache:
  paths:
    - .venv/

install_deps:
  stage: deps
  script:
    - pip install -r requirements.txt

test:
  stage: test
  script:
    - pytest --junitxml=reports/junit.xml
```

## 📋 TODO и планы развития

- [ ] Поддержка Kubernetes конфигураций (Helm charts)
- [ ] Интеграция с GitHub Actions
- [ ] Веб-интерфейс
- [ ] Анализ безопасности зависимостей
- [ ] Поддержка монорепозиториев
- [ ] Генерация documentation pipeline

## 🐛 Известные проблемы

- На Windows могут возникать проблемы с длинными путями при клонировании некоторых репозиториев
- Большие репозитории (>1GB) могут клониться медленно
- Некоторые приватные зависимости могут не определяться корректно

## 📞 Поддержка

- Создайте Issue в GitHub для сообщения о багах
- Используйте Discussions для вопросов и предложений
- Проверьте FAQ в Wiki проекта

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE) для деталей.

---

**Made with ❤️ for DevOps Engineers**

*Автоматизируйте CI/CD, а не создавайте их вручную!*
